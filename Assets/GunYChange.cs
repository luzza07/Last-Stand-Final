using UnityEngine;

public class GunYChange : MonoBehaviour
{
    //void Update()
    //{
    //    // Check if player is pressing 'A' (for X and Y rotation = 180 degrees)
    //    if (Input.GetKey(KeyCode.A))
    //    {
    //        transform.rotation = Quaternion.Euler(180, 0, 180);  // Rotate 180 degrees around the X and Y axes
    //    }
    //    // Check if player is pressing 'D' (for X and Y rotation = 0 degrees)
    //    else if (Input.GetKey(KeyCode.D))
    //    {
    //        transform.rotation = Quaternion.Euler(180, 0, 0);  // Rotate to 180 degrees for X, 0 degrees for Y
    //    }
    //}
}
